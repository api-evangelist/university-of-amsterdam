openapi: 3.2.0
info:
  title: LiteLLM [beta] Anthropic Event Logging API
  description: 'Enterprise Edition


    Proxy Server to call 100+ LLMs in the OpenAI format. **Customize Swagger Docs**


    👉 ```LiteLLM Admin Panel on /ui```. Create, Edit Keys with SSO. Having issues? Try ```Fallback Login```


    💸 ```LiteLLM Model Cost Map```.


    🔎 ```LiteLLM Model Hub```. See available models on the proxy. **Docs**'
  version: 1.95.0
tags:
- name: '[beta] Anthropic Event Logging'
paths:
  /api/event_logging/batch:
    post:
      tags:
      - '[beta] Anthropic Event Logging'
      summary: Event Logging Batch
      description: 'Stubbed endpoint for Anthropic event logging batch requests.


        This endpoint accepts event logging requests but does nothing with them.

        It exists to prevent 404 errors from Claude Code clients that send telemetry.'
      operationId: event_logging_batch_api_event_logging_batch_post
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
components:
  securitySchemes:
    APIKeyHeader:
      type: apiKey
      description: Bearer token
      in: header
      name: x-litellm-api-key
